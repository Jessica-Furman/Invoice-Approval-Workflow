**Checkbox** — a binary toggle within a form; use for multi-select and opt-ins.

```jsx
<Checkbox label="Email me product updates" defaultChecked />
```

Controlled via `checked`/`onChange` or uncontrolled via `defaultChecked`.
