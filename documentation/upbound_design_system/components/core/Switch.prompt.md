**Switch** — an instant on/off toggle; use for settings that apply immediately.

```jsx
<Switch label="Enable notifications" defaultChecked />
```

Controlled via `checked`/`onChange` or uncontrolled via `defaultChecked`.
