**Tabs** — an underline tab set for switching between views; active tab underlined in green.

```jsx
<Tabs tabs={["Overview", "Pricing", "FAQ"]} onChange={setTab} />
```

Controlled (`value`) or uncontrolled (`defaultValue`). Tabs accept strings or `{label,value}`.
