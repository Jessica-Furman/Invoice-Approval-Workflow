**Tag** — a pill-shaped label or filter chip; use for categories, filters, and removable selections.

```jsx
<Tag selected>Enterprise</Tag>
<Tag onRemove={() => {}}>Fintech</Tag>
```

`selected` raises it to the green accent; `onRemove` adds a × affordance.
