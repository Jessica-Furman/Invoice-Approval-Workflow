**Tooltip** — a small hover/focus label; use to clarify icon-only controls and truncated text.

```jsx
<Tooltip label="Copy link"><IconButton name="link" aria-label="Copy link" /></Tooltip>
```

`side` positions it `top | bottom | left | right`.
