**Toast** — a transient notification; use for confirmations and non-blocking alerts.

```jsx
<Toast tone="success" title="Saved" onClose={dismiss}>Your changes are live.</Toast>
```

Dark surface with an accent bar. Tones: `neutral | success | warning | danger`.
