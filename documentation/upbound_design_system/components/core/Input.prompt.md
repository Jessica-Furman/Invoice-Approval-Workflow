**Input** — a single-line text field with label, hint, and error states; use for forms and search. Focus shows the green ring.

```jsx
<Input label="Work email" placeholder="you@company.com" hint="We'll never share it." />
<Input label="Amount" error="Enter a valid number" />
```

Props: `label`, `hint`, `error`, `iconLeft`, plus native input attrs.
