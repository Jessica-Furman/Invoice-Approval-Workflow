**Textarea** — a multi-line text field; use for messages, notes, and long-form input.

```jsx
<Textarea label="How can we help?" rows={5} placeholder="Tell us more…" />
```

Props: `label`, `hint`, `error`, `rows`, plus native textarea attrs.
