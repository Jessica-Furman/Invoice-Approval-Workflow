**Radio / RadioGroup** — single choice from a set; use `RadioGroup` to manage the set by value.

```jsx
<RadioGroup value={plan} onChange={setPlan}
  options={["Starter", "Growth", "Enterprise"]} />
```

Standalone `<Radio label name value checked onChange />` is also available.
