**Card** — a flat content surface with a hairline border and soft shadow; use to group related content. Never add colored left-border accents.

```jsx
<Card interactive>
  <h3>Enterprise lending</h3>
  <p>Move your finances forward.</p>
</Card>
```

`interactive` adds a hover lift; `padding` overrides inner spacing.
